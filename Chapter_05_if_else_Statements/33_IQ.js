if([]){
    console.log("True!");
}

// [] - it is an object 

let response;
if(response){
// It accept 0, "" and false which might be legitimate API values
}
if(response !== null){
    // this is advice to use. It catched both null and undefined
}

// in QA strict equality (===)catches bug 
