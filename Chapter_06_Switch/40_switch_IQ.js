let x = 10;
switch (x) {
    case x:
        let b1 = 1;
        console.log(b1);
        break;
    case x:
        let b2 = 2;
        console.log(b2);
        break;
    default:
        console.log("d");
    // default:
    //     console.log("d");

}

// IT will allow you to have the duplicate case with first as the usage. 