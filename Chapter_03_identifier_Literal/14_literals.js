let age = "xyz"; //string literals
let isStudent = true; // boolean 
let pi = 3.14; // number 
let name = 'Alice';  // string 
let nullValue = null; //null literal assigned to the variable 'nullvalue'
let undefinedValue; //this variable is declared but not assigned, so it has the value 'undefined'

//template literals
let greeting = `Hello, ${name}`; //this is template literal that includes as expressions

